<h1>Users#show　マイページ</h1>
<p>Find me in app/views/users/show.html.erb</p>
<hr>
<p><span>email: </span><%= @user.email %></p>

<%= link_to "フォロー", follows_user_path(@user.id) %>
<%= link_to "フォロワー", followers_user_path(@user.id) %>
<p>（投稿一覧）</p>
<% @tweets.each do |tweet| %>
    <hr>

    <p><span>ツイート内容: </span><%=link_to tweet.body, tweet_path(tweet.id) %></p>
<% end %>
<hr><br>
<p>(いいね)</p><hr>

<% @iined_tweets.each do |tweet| %>
    <hr>
    <p><span>いいねしたツイート内容: </span><%=link_to tweet.body, tweet_path(tweet.id) %></p>
  <% end %>
